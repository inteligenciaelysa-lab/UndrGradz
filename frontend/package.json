{
  "name": "undrgradz-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "cap:sync": "npm run build && npx cap sync",
    "cap:run": "npm run cap:sync && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@capacitor/splash-screen": "^8.0.2",
    "puppeteer-core": "^25.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
